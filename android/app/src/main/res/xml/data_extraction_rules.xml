<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing is backed up.

  The offline cache can be hundreds of megabytes, and the stored session holds a
  refresh token: a device-bound credential that should not be copied to another
  device by a backup mechanism the reader never chose. Re-downloading a book is
  cheap; a leaked token is not.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
